# Copia a .env.tunnel y pega el token del túnel (Zero Trust → Networks → Tunnels).
# NO subas .env.tunnel a git.

CLOUDFLARE_TUNNEL_TOKEN=eyJhIjoi...

# Solo informativo (lo configuras en el panel de Cloudflare):
# PUBLIC_HOSTNAME=musica.tudominio.com
