# Copia a .env.deploy y ajusta si hace falta. NO subas .env.deploy a git.

# SSH al HP (misma red local)
DEPLOY_REMOTE=reichel@192.168.1.219
DEPLOY_BASE=/home/reichel/Pelis/music-catalog
DEPLOY_BRANCH=main

# Solo si no tienes clave SSH configurada (requiere: brew install sshpass)
# DEPLOY_SSH_PASS=tu_contraseña

# URL pública (solo informativa al terminar)
PUBLIC_URL=https://musica.reicheleria.com
